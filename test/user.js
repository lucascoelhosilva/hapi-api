'use strict'

const lab = exports.lab = require('lab').script();
const { expect } = require('code');

lab.describe('testes do resources users', () => {
    lab.test('deve retornar com sucesso', () => {
        expect(true).to.be.true();
    })
});